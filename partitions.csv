# Custom partition table for ESP32-C6 with 8MB flash
# Default IDF table only allocates 1 MB to the app, which the firmware
# outgrew once we added TLS + the public CA bundle. Here we give the
# factory app 3 MB - plenty of headroom for the foreseeable additions.
#
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  0x300000,
